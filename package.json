{
  "name": "pan-config-viewer-tests",
  "version": "1.0.0",
  "description": "Test suite for PAN Config Viewer API",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:device-groups": "jest tests/device-groups.test.js",
    "test:pagination": "jest tests/pagination.test.js",
    "test:pagination:verbose": "jest tests/pagination.test.js --verbose",
    "test:snapshot-update": "jest -u"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "axios": "^1.6.2"
  },
  "jest": {
    "testEnvironment": "node"
  }
}